# .env

# --- Main Instance Configuration ---
# This MUST be unique for each project to avoid Docker conflicts.
COMPOSE_PROJECT_NAME=frappe-project

# --- Frappe Version ---
# Corresponds to the Docker image tag. Check Docker Hub for available tags.
# Example: "3.10" for version-14, "3.11" for version-15
FRAPPE_PYTHON_TAG=3.11
FRAPPE_BRANCH=version-15

# --- Service Ports ---
# Change these if they conflict with other running services on your machine.
SITE_PORT=8000
SOCKETIO_PORT=9000
MARIADB_PORT=3307
REDIS_CACHE_PORT=13000
REDIS_QUEUE_PORT=11000
REDIS_SOCKETIO_PORT=12000